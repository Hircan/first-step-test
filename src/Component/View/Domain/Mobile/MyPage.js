import { Button } from "react-bootstrap";

export const MyInfo = () => {
    return (
        <>
            <Button>
                <span class="material-symbols-outlined">favorite</span>
                <span>프로필</span>
            </Button>
        </>
    );
};
